import React from "react";

const Loader = () => {
  return (
    <div
      className="loader"
      role="progressbar"
      aria-live="polite"
      aria-label="Loading..."
    ></div>
  );
};

export default Loader;
